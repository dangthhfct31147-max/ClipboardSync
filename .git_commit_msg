Fix service timeout: run host on dedicated background thread

_host.Start() is a blocking call that never returns until shutdown.
SCM requires OnStart to return quickly to confirm the service started.
Fix by running the entire host on a dedicated background thread so OnStart
returns immediately to the SCM.
